import { Box, styled } from '@mui/material';
export const CustomizeBox = styled(Box)(({}) => ({
    minHeight: '50px',
    width: '100%',
    backgroundColor: '#fff',
    border: '1px solid #d9d9d9',
    borderRadius: '12px',
    padding: '16px',
}));
